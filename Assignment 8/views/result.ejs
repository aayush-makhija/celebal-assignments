<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Job Matches</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      body {
        margin: 0;
        font-family: 'Inter', sans-serif;
        background: #f5f7fa;
        padding: 40px 20px;
      }

      h1 {
        text-align: center;
        margin-bottom: 30px;
        color: #333;
      }

      .jobs-container {
        max-width: 900px;
        margin: 0 auto;
        display: flex;
        flex-direction: column;
        gap: 20px;
      }

      .job-card {
        background-color: white;
        border-radius: 12px;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.05);
        padding: 24px;
        transition: transform 0.2s ease;
        border-left: 6px solid #4f46e5;
      }

      .job-card:hover {
        transform: scale(1.01);
      }

      .job-title {
        font-size: 20px;
        font-weight: 600;
        color: #1f2937;
        margin-bottom: 8px;
      }

      .company {
        font-size: 16px;
        color: #6b7280;
        margin-bottom: 12px;
      }

      .location {
        font-size: 14px;
        color: #374151;
        margin-bottom: 10px;
      }

      .salary {
        font-size: 14px;
        color: #10b981;
        margin-bottom: 10px;
      }

      .description {
        font-size: 14px;
        color: #4b5563;
        margin-bottom: 12px;
        line-height: 1.6;
      }

      .apply-btn {
        display: inline-block;
        padding: 10px 16px;
        background-color: #4f46e5;
        color: white;
        border-radius: 6px;
        text-decoration: none;
        font-size: 14px;
        font-weight: 500;
        transition: background-color 0.2s ease;
      }

      .apply-btn:hover {
        background-color: #4338ca;
      }

      .back-link {
        display: block;
        text-align: center;
        margin-top: 40px;
        color: #4f46e5;
        font-weight: 500;
        text-decoration: none;
      }

      .back-link:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <h1>Top Matching Jobs</h1>

    <div class="jobs-container">
      <% if (jobs.length === 0) { %>
      <p style="text-align: center; color: #888">
        No jobs found matching your resume. Try uploading a more detailed
        resume.
      </p>
      <% } %> <% jobs.forEach(job => { %>
      <div class="job-card">
        <div class="job-title"><%= job.title %></div>
        <div class="company">
          <%= job.company?.display_name || 'Unknown Company' %>
        </div>
        <div class="location">
          📍 <%= job.location?.display_name || 'N/A' %>
        </div>
        <% if (job.salary_min && job.salary_max) { %>
        <div class="salary">
          💰 £<%= job.salary_min %> - £<%= job.salary_max %>
        </div>
        <% } %>
        <div class="description">
          <%= job.description?.substring(0, 200) %>...
        </div>
        <a class="apply-btn" href="<%= job.redirect_url %>" target="_blank"
          >View & Apply</a
        >
      </div>
      <% }); %>
    </div>

    <a href="/" class="back-link">⬅ Upload Another Resume</a>
  </body>
</html>
